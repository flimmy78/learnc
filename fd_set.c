#include <stdio.h>

int main()
{
    do {
	//unsigned int __i;
	//fd_set *__arr = (fdset);
	//for (__i = 0; __i < sizeof(fd_set) / sizeof(__fd_mask); ++__i)
	     //((__arr)->__fds_bits)[__i] = 0;
	printf("hello fs_set\n");
    } while (0);
    return 0;
}
